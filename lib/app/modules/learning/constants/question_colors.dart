import 'package:flutter/material.dart';

/// Color constants for question templates
class QuestionColors {
  // Underline color
  static const Color underlineBorderColor = Color(0xFFD8D8D8);
  
  // Underline dimensions
  static const double underlineWidth = 1.5;
  
  // Widget dimensions
  static const double tagHeight = 5.0; // Multiplied by hp
  static const double tagHorizontalPadding = 2.5; // Multiplied by wp
  static const double tagBorderWidth = 2.0;
  static const double tagBorderRadius = 10.0;
  static const double tagFontSize = 13.0; // Multiplied by sp
}